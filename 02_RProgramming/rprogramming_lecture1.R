# add2 <- function(x,y){
#   x+y
# }
# 
# 
# above10<-function(x){
#   use<-x>2
#   x[use]
# }
# above<-function(x,n){
#   use<-x>n
#   x[use]
# }
# columnmean<-function(y){
#   nc<-ncol(y)
#   means<-numeric(nc)
#   for(i in 1:nc){
#     means[i]=mean(y[,i])
# }
# means
# }
# 
# f<-function(a,b){
#   a^2
# }
# 
# # x=read.csv("hw1_data.csv")
# # c=1
# # s<-matrix(rep(0,nrow(x)),nrow(x),1)
# # for (i in 1:nrow(x)){ 
# # if (x[i,][1]>31 && x[i,][4]>90 && !(is.na(x[i,][1])) && !(is.na(x[i,][4]))){
# #     s[i,][1]<-x[i,][2]
# #   }
# # }
# # mean(!(is.na(s)))
# 
# z=x[(!is.na(x[,][1]))][1] #
# mean(z[,])
# 
# max((x[(x[,][5]==5),][1])[!is.na((x[(x[,][5]==5),][1]))])
# 
# 
